import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onlytips.app',
  appName: 'Only Tips',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;